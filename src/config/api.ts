const apiConfig = {
  apiUrl: import.meta.env.VITE_API_URL,
  apiTokenIdentifier: "mycash@apiToken",
  userProfileIdentifier: "mycash@userProfile",
  defaultPageSize: 10,
};

export default apiConfig;
